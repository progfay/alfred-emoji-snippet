{
  "name": "emojisyo",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "prebuild": "npm run cleanup",
    "build": "npm run build:snippet && npm run build:zip",
    "build:snippet": "node src/index.js",
    "build:zip": "zip -jmq dist/Emoji.alfredsnippets dist/*.json",
    "cleanup": "rm -f dist/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "emojilib": "^2.4.0",
    "file-saver": "^2.0.1",
    "jszip": "^3.2.0",
    "uuid": "^3.3.2"
  }
}
